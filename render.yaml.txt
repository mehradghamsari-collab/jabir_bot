services:
  - type: worker
    name: jabir-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python jabir_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false